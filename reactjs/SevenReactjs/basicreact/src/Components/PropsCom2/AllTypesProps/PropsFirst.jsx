import React from 'react'

const PropsFirst = (props) => {
    // console.log(props)
  return (
    <div>
        <p>firstway props: {props.name}</p>
        <p>firstway props: {props.age}</p>
    </div>
  )
}

export default PropsFirst
