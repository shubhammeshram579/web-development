import React from 'react'

const PropsFith = (props) => {
    // console.log(props)
  return (
    <div>
      <div>
        <p>{props.data.bottel}</p>
        <p>{props.data.mouse}</p>
        <p>{props.data.keabord}</p>
        <p>{props.num}</p>
      </div>
    </div>
  )
}

export default PropsFith
